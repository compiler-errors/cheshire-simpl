fn main() -> Int {
  let s = 1.
  let t = 100.

  while t > 0 {
    let k = 1000.
    s = s * k.
    t = t - 1.
  }

  return s.
}

fn test_variables(arr: [Int], i: UInt) -> Int {
  let arr_i = arr[i].
  return arr_i.
}
